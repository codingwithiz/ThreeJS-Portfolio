// Routing now lives in main.jsx (BrowserRouter). This thin alias keeps the
// historical entry point valid; the home page composition is in pages/Home.jsx.
export { default } from './pages/Home.jsx';
